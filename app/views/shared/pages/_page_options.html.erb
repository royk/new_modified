<% if current_user && current_user.admin?%>
	<span class="pull-right">
		<li class="dropdown">
			<a href="#" class="dropdown-toggle" data-toggle="dropdown">
				<i class="icon-cog"></i>
			</a>
			<ul class="dropdown-menu">
				<li><%= link_to "Edit", edit_page_path(@page.name) %></li>
			</ul>
		</li>
	</span>
<% end %>