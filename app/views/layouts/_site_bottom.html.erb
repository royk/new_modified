		<%= debug(params) if Rails.env.development? %>
	</div>
	<%= render 'layouts/footer' %>
	<script type="text/javascript">

  var _gaq = _gaq || [];
  _gaq.push(['_setAccount', 'UA-37345788-1']);
  _gaq.push(['_trackPageview']);

  (function() {
    var ga = document.createElement('script'); ga.type = 'text/javascript'; ga.async = true;
    ga.src = ('https:' == document.location.protocol ? 'https://ssl' : 'http://www') + '.google-analytics.com/ga.js';
    var s = document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(ga, s);
  })();

</script>
<script>
	$(document).ready(function() {
		var datePickers = $(".datepicker");
		if (datePickers.length>0) {
			datePickers.datepicker({maxDate:"0d"});
		}
	});

</script>
<script type="text/javascript">
  $(document).ready(function() {
    tinyMCE.init({
        // General options
        mode : "specific_textareas",
        editor_selector: "tinymce-mini",
        theme : "advanced",
        theme_advanced_buttons1 : "bold,italic,underline,|,bullist,numlist,|,outdent,indent,|,image,link,unlink,|,code",
        // Theme options
        theme_advanced_resizing : true,
        width: "100%",
        height: 50,
        theme_advanced_resizing_min_height: 50,
        theme_advanced_resize_horizontal: false
    });
  });
</script>
</body>
</html>
